It needs a pool that keeps running app instances warm before assigning them.
This makes startup very fast, instead of purely cold start.
Or would startup be fast anyway since it does not need to pull the image? I think so actually.

So perhaps we don't need a pool?

But still it needs to start the openclaw gateway so may be faster still.

But first we need to just get the system actually working before we optimise.

DO NOT PREMATURELY OPTIMIZE.

Is there a system that does this already?
